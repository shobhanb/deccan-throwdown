server {
    listen 80;
    server_name cfmf.cfgames.site itcf.cfgames.site enduro.cfgames.site 9one.cfgames.site kaali.cfgames.site blackfire.cfgames.site;

    # Redirect all HTTP requests to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }

    # Allow certbot challenge responses
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

# CFMF subdomain
server {
    listen 443 ssl;
    server_name cfmf.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/cfmf.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cfmf.cfgames.site/privkey.pem;

    # Serve CFMF-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /cfmf.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/cfmf/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/cfmf/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}

# ITCF subdomain
server {
    listen 443 ssl;
    server_name itcf.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/itcf.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/itcf.cfgames.site/privkey.pem;

    # Serve ITCF-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /itcf.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/itcf/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/itcf/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}

# enduro subdomain
server {
    listen 443 ssl;
    server_name enduro.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/enduro.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/enduro.cfgames.site/privkey.pem;

    # Serve enduro-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /enduro.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/enduro/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/enduro/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}

# 9one subdomain
server {
    listen 443 ssl;
    server_name 9one.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/9one.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/9one.cfgames.site/privkey.pem;

    # Serve 9one-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /9one.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/9one/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/9one/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}

# kaali subdomain
server {
    listen 443 ssl;
    server_name kaali.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/kaali.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kaali.cfgames.site/privkey.pem;

    # Serve kaali-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /kaali.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/kaali/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/kaali/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}

# blackfire subdomain
server {
    listen 443 ssl;
    server_name blackfire.cfgames.site;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/blackfire.cfgames.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/blackfire.cfgames.site/privkey.pem;

    # Serve blackfire-specific items
    location = /manifest.webmanifest {
        root /usr/share/nginx/html;
        try_files /blackfire.manifest.webmanifest =404;
        add_header Content-Type application/manifest+json;
    }
    location = /icons/logo_192.jpg {
        root /usr/share/nginx/html;
        try_files /icons/blackfire/logo_192.jpg /icons/common/logo_192.jpg =404;
    }
    location = /icons/logo_512.jpg {
        root /usr/share/nginx/html;
        try_files /icons/blackfire/logo_512.jpg /icons/common/logo_512.jpg =404;
    }

    # Frontend location
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    # Backend API proxy
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr; 
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass_request_headers on;
    }
}
